<article class="px-8 py-6 rounded-xl border border-wine-600">
    <header class="text-2xl text-wine-600 font-semibold mb-4">{{ $titulo }}</header>
    <main class="flex justify-between items-center text-white">
        <div class="space-y-2">
            <h5 class="font-semibold text-lg">Cuenta Corriente</h5>
            <p class="text-sm">{{ $cuenta }}</p>
        </div>
        <div class="space-y-2">
            <h5 class="font-semibold text-lg">CCI</h5>
            <p class="text-sm">{{ $cci }}</p>
        </div>
    </main>
</article>
